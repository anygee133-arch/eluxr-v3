---
phase: 01-security-hardening-database-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n-workflow-remote
autonomous: false
user_setup:
  - service: n8n-cloud
    why: "Credential creation requires dashboard access (security-sensitive, no API)"
    dashboard_config:
      - task: "Create Header Auth credential named 'KIE AI API'"
        location: "n8n Cloud (flowbound.app.n8n.cloud) -> Credentials -> Add Credential -> Header Auth"
        details: "Header Name: Authorization, Header Value: Bearer 7f48c3109ae4ee6aee94ba7389bdcaa4"
      - task: "Create Supabase credential named 'Supabase Service Role'"
        location: "n8n Cloud -> Credentials -> Add Credential -> Supabase API"
        details: "If native Supabase credential exists, use it. Otherwise create Header Auth with Header Name: apikey, Header Value: <service_role_key from Supabase Dashboard -> Project Settings -> API>"
  - service: supabase
    why: "Retrieve service_role key for n8n credential"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key (keep secret!)"

must_haves:
  truths:
    - "The KIE API key 7f48c3109ae4ee6aee94ba7389bdcaa4 does not appear anywhere in any n8n workflow JSON"
    - "All 5 KIE HTTP Request nodes use the n8n credential store via predefined credential reference"
    - "The Supabase service_role key is stored in the n8n credential store"
  artifacts:
    - path: "n8n credential: KIE AI API"
      provides: "Secure storage for KIE AI Bearer token"
    - path: "n8n credential: Supabase Service Role"
      provides: "Secure storage for Supabase service_role key"
    - path: "n8n workflow: ELUXR social media Action v2"
      provides: "Updated workflow with credential references instead of hardcoded keys"
  key_links:
    - from: "KIE AI API credential"
      to: "5 HTTP Request nodes (KIE -- Create Image Task, KIE -- Get Image Result, KIE -- Create Video Task, KIE -- Get Video Status, KIE -- Generate Content Image)"
      via: "predefinedCredentialType: httpHeaderAuth"
      pattern: "httpHeaderAuth"
    - from: "workflow JSON"
      to: "zero matches for 7f48c3109ae4ee6aee94ba7389bdcaa4"
      via: "grep/search"
      pattern: "7f48c3109ae4ee6aee94ba7389bdcaa4"
---

<objective>
Migrate the hardcoded KIE API key from 5 n8n HTTP Request nodes into the n8n credential store, and set up the Supabase service_role credential for future backend writes.

Purpose: Zero hardcoded secrets in any workflow JSON. The KIE key must be secured BEFORE Phase 3 workflow splitting to prevent propagating the plaintext key into 13 sub-workflows. The Supabase credential is needed for all future n8n-to-Supabase writes starting in Phase 3.

Output: Two n8n credentials created, 5 workflow nodes updated to reference the KIE credential, and zero hardcoded API keys remaining in the workflow JSON.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-hardening-database-foundation/01-RESEARCH.md
@ELUXR social media Action v2 (3).json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create KIE AI and Supabase credentials in n8n dashboard</name>
  <action>
The user must create two credentials in the n8n Cloud dashboard (flowbound.app.n8n.cloud):

**Credential 1: KIE AI API**
1. Go to flowbound.app.n8n.cloud -> Credentials -> Add Credential
2. Search for "Header Auth" (generic credential type)
3. Name: `KIE AI API`
4. Header Name: `Authorization`
5. Header Value: `Bearer 7f48c3109ae4ee6aee94ba7389bdcaa4`
6. Save the credential
7. Note the credential ID shown in the URL after saving

**Credential 2: Supabase Service Role**
1. First, get the service_role key from Supabase Dashboard -> Project Settings -> API -> service_role key
2. Go to n8n Cloud -> Credentials -> Add Credential
3. Search for "Supabase API" (if native type exists) or "Header Auth"
4. If using native Supabase credential:
   - Name: `Supabase Service Role`
   - Host: `https://llpnwaoxisfwptxvdfed.supabase.co`
   - Service Role Key: (paste the key)
5. If using Header Auth:
   - Name: `Supabase Service Role`
   - Header Name: `apikey`
   - Header Value: (paste the service_role key)
6. Save the credential
  </action>
  <resume-signal>Provide the credential IDs for both "KIE AI API" and "Supabase Service Role" (visible in the n8n URL after saving each credential), then type "credentials created" to continue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update all 5 KIE HTTP Request nodes to use credential store and verify</name>
  <files>n8n-workflow-remote</files>
  <action>
After the user provides the KIE AI API credential ID, update the n8n workflow to remove the hardcoded key from all 5 KIE nodes.

**Nodes to update (5 total):**

1. **KIE -- Create Image Task** (ID: `2c44ddce-f441-499d-bc61-95e49da42316`)
   - Change: authentication from inline header to predefined credential
   - Set: `authentication` to `predefinedCredentialType`
   - Set: `nodeCredentialType` to `httpHeaderAuth`
   - Set: credential reference to "KIE AI API" (using the credential ID from Task 1)
   - Remove: the `Authorization` entry from `headerParameters.parameters` array
   - Keep: `Content-Type: application/json` header if present

2. **KIE -- Get Image Result** (ID: `4f2a54ae-b572-44e1-a39a-acdc3435f4c2`)
   - Same changes as above

3. **KIE -- Create Video Task** (at line 340 in workflow JSON)
   - Same changes as above

4. **KIE -- Get Video Status** (ID: `83a8020f-d588-4480-a22b-6afae6f9b5ed`)
   - Same changes as above

5. **KIE -- Generate Content Image** (ID: `c4368be5-2b98-4905-a7a9-cb091162483b`)
   - Same changes as above

**For each node, the JSON structure changes from:**
```json
{
  "authentication": "none",
  "options": {
    "headerParameters": {
      "parameters": [
        {
          "name": "Authorization",
          "value": "Bearer 7f48c3109ae4ee6aee94ba7389bdcaa4"
        }
      ]
    }
  }
}
```

**To:**
```json
{
  "authentication": "predefinedCredentialType",
  "nodeCredentialType": "httpHeaderAuth",
  "options": {}
}
```

With the node's `credentials` section updated to:
```json
{
  "credentials": {
    "httpHeaderAuth": {
      "id": "<KIE_CREDENTIAL_ID>",
      "name": "KIE AI API"
    }
  }
}
```

**Implementation approach:**
- Use the n8n MCP tools (`n8n_create_workflow` or equivalent) to push the updated workflow
- OR: Download the workflow JSON, modify all 5 nodes locally, re-upload via MCP
- OR: Edit each node individually via the n8n API if available

**Post-update verification:**
1. Export the workflow JSON from n8n
2. Search the entire JSON for `7f48c3109ae4ee6aee94ba7389bdcaa4` -- must return ZERO matches
3. Search for `Bearer ` in header parameters -- must return ZERO matches in node parameters (only in credential references)
4. Verify all 5 KIE nodes have `"authentication": "predefinedCredentialType"` and reference the KIE AI API credential
5. Test one node (e.g., KIE -- Create Image Task) with a simple prompt to confirm the credential works
  </action>
  <verify>
1. Export workflow JSON and search for `7f48c3109ae4ee6aee94ba7389bdcaa4` returns zero matches
2. All 5 KIE nodes have `authentication: predefinedCredentialType` in their JSON
3. All 5 KIE nodes reference the `KIE AI API` credential by ID
4. At least one KIE node tested successfully (API call returns 200)
  </verify>
  <done>
The KIE API key `7f48c3109ae4ee6aee94ba7389bdcaa4` no longer appears anywhere in the n8n workflow JSON. All 5 KIE HTTP Request nodes use the n8n credential store. The Supabase service_role key is also stored in the n8n credential store for future use in Phase 3.
  </done>
</task>

</tasks>

<verification>
1. Search n8n workflow JSON for `7f48c3109ae4ee6aee94ba7389bdcaa4` returns zero matches
2. All 5 KIE nodes use `predefinedCredentialType: httpHeaderAuth` with credential "KIE AI API"
3. "Supabase Service Role" credential exists in n8n credential store
4. At least one KIE API call succeeds using the credential (not hardcoded key)
5. No other hardcoded API keys found in the workflow JSON (Anthropic, Perplexity, Google already verified safe)
</verification>

<success_criteria>
- Zero occurrences of `7f48c3109ae4ee6aee94ba7389bdcaa4` in any n8n workflow JSON
- All 5 KIE HTTP Request nodes reference the "KIE AI API" credential from the n8n credential store
- "Supabase Service Role" credential exists in n8n and is ready for Phase 3
- At least one KIE node verified working with the new credential
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening-database-foundation/01-02-SUMMARY.md`
</output>
