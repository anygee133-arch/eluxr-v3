---
phase: 06-content-pipeline
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - workflows/01-icp-analyzer.json
autonomous: true
requirements: [PIPE-01]

must_haves:
  truths:
    - "ICP Analyzer scrapes the user's website via Jina Reader API and extracts products"
    - "Products are stored in the Supabase products table per-tenant"
    - "Perplexity performs 2-3 market research calls (industry landscape, audience pain points, content gaps)"
    - "Claude synthesizes all research into a professional ICP with demographics, pain points, desires, content pillars, hashtags"
    - "ICP is stored in the Supabase icps table with all enriched JSONB fields"
    - "ICP Analyzer calls the orchestrator callback on completion"
  artifacts:
    - path: "workflows/01-icp-analyzer.json"
      provides: "Complete ICP analysis workflow with Jina + Perplexity + Claude + product extraction"
      contains: "r.jina.ai"
  key_links:
    - from: "workflows/01-icp-analyzer.json"
      to: "Supabase products table"
      via: "HTTP Request POST to /rest/v1/products"
      pattern: "rest/v1/products"
    - from: "workflows/01-icp-analyzer.json"
      to: "Supabase icps table"
      via: "HTTP Request POST/PATCH to /rest/v1/icps"
      pattern: "rest/v1/icps"
    - from: "workflows/01-icp-analyzer.json"
      to: "Orchestrator callback"
      via: "HTTP POST to callback_url with step=1, status=complete"
      pattern: "callback_url"
---

<objective>
Overhaul the 01-ICP-Analyzer workflow to implement the three-service ICP pipeline: Jina AI (site scraping + product extraction) -> Perplexity (2-3 market research calls) -> Claude (ICP synthesis). Store products and enriched ICP in Supabase.

Purpose: PIPE-01 requires ICP analysis via Perplexity + Claude stored per-tenant. The user's locked decision specifies Jina AI for scraping, products stored in a new products table, and Claude synthesizing demographics, pain points, content pillars, and hashtags.

Output: Updated 01-icp-analyzer.json workflow file
</objective>

<execution_context>
@/Users/andrewgershan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewgershan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-pipeline/06-CONTEXT.md
@.planning/phases/06-content-pipeline/06-RESEARCH.md
@.planning/phases/06-content-pipeline/06-01-SUMMARY.md

@workflows/01-icp-analyzer.json
@workflows/14-pipeline-orchestrator.json
@config.js

<interfaces>
<!-- Orchestrator callback contract (from Plan 01) -->
Callback URL: POST to callback_url from payload
Callback body: { pipeline_run_id, step: 1, status: "complete"|"error", error_message?: string, data?: object }
Callback header: X-Pipeline-Secret: "eluxr-pipeline-internal-2024"

<!-- Supabase products table (from Plan 01 migration) -->
POST /rest/v1/products
Body: [{ user_id, name, description, price, features, image_url, category, source_url, is_active }]

<!-- Supabase icps table (enriched columns from Plan 01 migration) -->
POST /rest/v1/icps?on_conflict=user_id with Prefer: resolution=merge-duplicates
Body: { user_id, business_url, industry, icp_summary, demographics, psychographics, content_preferences, competitors, content_opportunities, recommended_hashtags, raw_research, content_pillars, pain_points, desires, objections, buying_triggers }

<!-- Existing credential IDs -->
Claude API credential: cZwkXj4ZfHTkpBtT
Perplexity API credential: 3rYRY1C2K9o0DDXI
Supabase auth pattern: dual headers (Authorization: Bearer SERVICE_ROLE_KEY, apikey: SERVICE_ROLE_KEY)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Jina scraping + Claude product extraction pipeline</name>
  <files>workflows/01-icp-analyzer.json</files>
  <action>
Update the 01-ICP-Analyzer workflow. **Keep the existing Auth Validator chain** (Webhook -> Auth Validator -> Auth OK? -> 401 Unauthorized). Replace everything after the Auth OK? true path with the new pipeline.

**New flow after Auth OK? (true path):**

1. **"Prepare Jina URL" (Code node):** Extract `business_url` from `$json.body.business_url` (webhook body). Build Jina Reader URL: `https://r.jina.ai/${business_url}`. Also extract `user_id` from `$json.user_id`, `industry` from `$json.body.industry`, `month` from `$json.body.month`, `platforms` from `$json.body.platforms`, `pipeline_run_id` from `$json.body.pipeline_run_id`, `callback_url` from `$json.body.callback_url`. Pass all forward.

2. **"Scrape Website" (HTTP Request node):** GET to the Jina URL (from step 1). Set timeout to 30000ms. Set `neverError: true` to handle scrape failures gracefully. No auth needed (Jina Reader free tier: 20 RPM without API key). Add header `Accept: application/json` for structured output.

3. **"Extract Products" (Code node -> HTTP Request node for Claude):** Prepare Claude API call to extract products from Jina output. The Code node prepares the request body:
   - Model: `claude-sonnet-4-20250514`
   - Max tokens: 4096
   - System prompt: "You are a product/service extractor. Analyze website content and extract ALL products, services, or offerings. If the site is not e-commerce, treat service packages, consulting tiers, software plans, or menu items as products."
   - User message includes: the Jina markdown output (truncated to first 12000 chars), instruction to return JSON array with fields: name, description, price, features (string[]), image_url, category
   - Must say "Return ONLY a valid JSON array. No markdown, no explanation."

   HTTP Request node calls Claude API at `https://api.anthropic.com/v1/messages` with credential `cZwkXj4ZfHTkpBtT`. Use predefinedCredentialType for Claude.

4. **"Parse Products" (Code node):** Parse Claude's response using robust JSON parsing (strip markdown fences, extract JSON array). Handle empty/failed extraction gracefully (default to empty array). Map each product to the products table schema: `{ user_id, name, description, price, features, image_url, category, source_url: business_url, is_active: true }`.

5. **"Delete Old Products" (HTTP Request node):** DELETE from Supabase `/rest/v1/products?user_id=eq.{user_id}` to clear previous scrape. Use service_role auth headers.

6. **"Save Products" (HTTP Request node):** POST to `/rest/v1/products` with the parsed products array. Batch insert. Use service_role auth headers. Add `Prefer: return=representation` header. Skip if products array is empty.

7. **"Perplexity Research 1" (Code node -> HTTP Request):** Prepare Perplexity API call for industry landscape + competitor analysis. Use model `sonar-pro`. Prompt: "Research the {industry} industry. Provide: 1) Key market trends and growth areas, 2) Top 5-10 competitors for a business like {business_url}, 3) Industry-specific content strategies that perform well on social media. Include citations."

8. **"Perplexity Research 2" (Code node -> HTTP Request):** Audience pain points + desires. Prompt: "Research the target audience for {industry} businesses like {business_url}. Provide: 1) Top pain points and frustrations, 2) Desires and aspirations, 3) Common objections to purchasing, 4) Buying triggers, 5) Where this audience spends time online. Include citations."

9. **"Perplexity Research 3" (Code node -> HTTP Request):** Content gaps + opportunities. Prompt: "Research content marketing opportunities in the {industry} space for {business_url}. Provide: 1) Underserved content topics, 2) High-engagement content formats, 3) 15-20 trending and niche hashtags for LinkedIn, Instagram, X, and YouTube, 4) Content pillars that resonate with the target audience. Include citations."

   For all 3 Perplexity calls: use credential `3rYRY1C2K9o0DDXI`, endpoint `https://api.perplexity.ai/chat/completions`, model `sonar-pro`.

10. **"Synthesize ICP" (Code node -> HTTP Request for Claude):** Combine all research (Jina site content summary, 3 Perplexity responses, extracted products list) into a comprehensive Claude prompt. Ask Claude to synthesize a professional ICP as a JSON object:
    ```json
    {
      "icp_summary": "2-3 paragraph executive summary",
      "demographics": { "age_range": "", "gender": "", "income_level": "", "education": "", "location": "", "job_titles": [] },
      "psychographics": { "values": [], "interests": [], "lifestyle": "" },
      "pain_points": ["..."],
      "desires": ["..."],
      "objections": ["..."],
      "buying_triggers": ["..."],
      "content_pillars": [{ "name": "", "description": "", "example_topics": [] }],
      "content_preferences": { "platforms": {}, "content_types": [], "best_posting_times": {} },
      "competitors": [{ "name": "", "url": "", "strengths": [], "weaknesses": [] }],
      "content_opportunities": ["..."],
      "recommended_hashtags": { "linkedin": [], "instagram": [], "x": [], "youtube": [] }
    }
    ```
    Use Claude credential. Max tokens 8192.

11. **"Parse ICP" (Code node):** Robust JSON parsing of Claude's ICP response (strip markdown fences, regex extract JSON object). Map to icps table columns.

12. **"Save ICP" (HTTP Request node):** UPSERT to `/rest/v1/icps?on_conflict=user_id` with `Prefer: resolution=merge-duplicates,return=representation`. Include all enriched fields (content_pillars, pain_points, desires, objections, buying_triggers plus existing fields).

13. **"Callback to Orchestrator" (HTTP Request node):** POST to `callback_url` (from step 1) with body `{ pipeline_run_id, step: 1, status: "complete" }` and header `X-Pipeline-Secret: eluxr-pipeline-internal-2024`. Set `neverError: true`.

14. **"Respond Success" (Respond to Webhook node):** Return `{ success: true, icp_id, products_count }` with 200 status. This is needed for direct calls (not via orchestrator).

**Error handling:** Wrap the pipeline in a try/catch pattern. If any step fails critically, call the callback with `status: "error"` and `error_message`, then respond with error. Use `neverError: true` on all HTTP Request nodes and check response status in Code nodes.

**Important n8n JSON patterns to follow:**
- Use `executeWorkflowTrigger` v1 for Auth Validator (not v1.1)
- Use IF node v2.2 with typeValidation: strict for auth check
- Code nodes use `$input.item.json` (default mode)
- HTTP Request nodes use typeVersion 4.2
- Set `alwaysOutputData: true` on HTTP Request nodes that might return empty
- Node IDs follow prefix pattern: `01-icp-XXXX`
  </action>
  <verify>
    <automated>cd /Users/andrewgershan/Projects/eluxr-v3 && node -e "const wf = require('./workflows/01-icp-analyzer.json'); const nodes = wf.nodes.map(n => n.name); console.log('Nodes:', nodes.length); const hasJina = nodes.some(n => n.includes('Scrape')); const hasPerplexity = nodes.filter(n => n.includes('Perplexity') || n.includes('Research')).length >= 2; const hasClaude = nodes.filter(n => n.includes('Claude') || n.includes('Synthesize') || n.includes('Extract Products')).length >= 1; const hasCallback = nodes.some(n => n.includes('Callback')); console.assert(hasJina, 'Missing Jina scraping'); console.assert(hasPerplexity, 'Missing Perplexity research calls'); console.assert(hasClaude, 'Missing Claude synthesis'); console.assert(hasCallback, 'Missing callback to orchestrator'); console.log('PASS');"</automated>
  </verify>
  <done>01-ICP-Analyzer workflow has: Jina Reader scraping node, Claude product extraction, 3 Perplexity research calls, Claude ICP synthesis, products saved to Supabase, ICP upserted to Supabase with all enriched fields, callback to orchestrator on completion, error handling with callback on failure.</done>
</task>

</tasks>

<verification>
1. Workflow JSON is valid and parseable
2. All 3 services integrated: Jina (HTTP GET), Perplexity (3 calls), Claude (2 calls)
3. Products saved to products table with batch INSERT
4. ICP upserted with all new JSONB fields
5. Callback fires on both success and error paths
6. Robust JSON parsing on all Claude responses
</verification>

<success_criteria>
- ICP Analyzer takes business_url + industry and produces enriched ICP + products list
- Products stored per-tenant in products table
- ICP stored with content_pillars, pain_points, desires, objections, buying_triggers
- Orchestrator callback fires with step=1 status on completion
- Entire execution completes within 5 minutes (Jina ~10s, 3x Perplexity ~30s, 2x Claude ~20s, DB ops ~5s)
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-pipeline/06-02-SUMMARY.md`
</output>
