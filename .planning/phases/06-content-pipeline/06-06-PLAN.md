---
phase: 06-content-pipeline
plan: 06
type: execute
wave: 5
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05]
files_modified: []
autonomous: false
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04, PIPE-05, PIPE-06]

must_haves:
  truths:
    - "User enters business URL and industry, triggers generation, and receives ICP analysis stored in icps table"
    - "ICP feeds into theme generation producing 4 weekly shows per month stored in themes table"
    - "Content generation produces 1 post per platform per day with platform-specific formatting"
    - "Each text post has an AI-generated hero image stored as URL in content_items"
    - "Video-eligible posts have structured scripts with trending audio stored in content_items"
    - "Products card shows scraped products on Setup tab"
    - "Pipeline completes all 6 steps via callback pattern without timeout"
  artifacts:
    - path: "workflows/14-pipeline-orchestrator.json"
      provides: "Callback-pattern orchestrator"
    - path: "workflows/01-icp-analyzer.json"
      provides: "Jina + Perplexity + Claude ICP pipeline"
    - path: "workflows/02-theme-generator.json"
      provides: "Netflix model theme generation"
    - path: "workflows/04-content-studio.json"
      provides: "Weekly batch content + images + video scripts"
    - path: "supabase/migrations/20260303_add_products_and_pipeline_columns.sql"
      provides: "Products table and column additions"
  key_links:
    - from: "index.html"
      to: "Orchestrator webhook"
      via: "authenticatedFetch POST to eluxr-generate-content"
      pattern: "eluxr-generate-content"
    - from: "Orchestrator"
      to: "Sub-workflows via callback"
      via: "Fire-and-forget + callback webhook"
      pattern: "eluxr-pipeline-callback"
---

<objective>
Verify the complete content pipeline end-to-end: from user entering a business URL to receiving a full month of content with images and video scripts. Deploy updated workflows to n8n Cloud and run the migration on Supabase.

Purpose: This is the integration verification plan. All 5 prior plans built individual components. This plan confirms they work together as a complete pipeline, deploys to production, and gets user confirmation.

Output: Deployed and verified content pipeline
</objective>

<execution_context>
@/Users/andrewgershan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewgershan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-pipeline/06-CONTEXT.md
@.planning/phases/06-content-pipeline/06-RESEARCH.md
@.planning/phases/06-content-pipeline/06-01-SUMMARY.md
@.planning/phases/06-content-pipeline/06-02-SUMMARY.md
@.planning/phases/06-content-pipeline/06-03-SUMMARY.md
@.planning/phases/06-content-pipeline/06-04-SUMMARY.md
@.planning/phases/06-content-pipeline/06-05-SUMMARY.md

@workflows/14-pipeline-orchestrator.json
@workflows/01-icp-analyzer.json
@workflows/02-theme-generator.json
@workflows/04-content-studio.json
@config.js
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run migration, deploy workflows, and verify integration</name>
  <files></files>
  <action>
**Step 1: Run Supabase Migration**
Execute the migration SQL from `supabase/migrations/20260303_add_products_and_pipeline_columns.sql` against the Supabase database. Use the Supabase CLI if available, or use the Supabase dashboard SQL editor via the REST API.

Verify migration success:
- Confirm `products` table exists with correct columns
- Confirm `content_items` has new columns (product_id, video_script, hashtags, first_comment)
- Confirm `campaigns` has show_name column
- Confirm `themes` has season_arc and inspirational_theme columns
- Confirm `icps` has new JSONB columns (content_pillars, pain_points, desires, objections, buying_triggers)
- Confirm RLS policies on products table

**Step 2: Deploy Workflow JSONs to n8n Cloud**
The 4 updated workflow files need to be imported/updated on n8n Cloud (flowbound.app.n8n.cloud):
1. `14-pipeline-orchestrator.json` -- deactivate existing, import new, activate
2. `01-icp-analyzer.json` -- deactivate existing, import new, activate
3. `02-theme-generator.json` -- deactivate existing, import new, activate
4. `04-content-studio.json` -- deactivate existing, import new, activate

Since n8n Cloud API key is not available (per STATE.md known constraint), this will be a checkpoint:human-action step for the user to import via the n8n Cloud dashboard.

**Step 3: Verify Each Endpoint is Active**
For each updated webhook, curl without auth to confirm it returns 401 (proving the workflow is active and auth is working):
```bash
curl -s -o /dev/null -w "%{http_code}" https://flowbound.app.n8n.cloud/webhook/eluxr-generate-content
curl -s -o /dev/null -w "%{http_code}" https://flowbound.app.n8n.cloud/webhook/eluxr-phase1-analyzer
curl -s -o /dev/null -w "%{http_code}" https://flowbound.app.n8n.cloud/webhook/eluxr-phase2-themes
curl -s -o /dev/null -w "%{http_code}" https://flowbound.app.n8n.cloud/webhook/eluxr-phase4-studio
curl -s -o /dev/null -w "%{http_code}" https://flowbound.app.n8n.cloud/webhook/eluxr-pipeline-callback
```

All should return 401 except the callback webhook (which may return 200 or 500 without proper payload -- that's expected).

**Step 4: Code Verification Checks**
Run automated checks on the codebase:
1. Verify orchestrator has 2 webhooks (entry + callback)
2. Verify ICP analyzer has Jina, Perplexity, Claude nodes
3. Verify theme generator has ICP read, products read, campaign upsert, theme insert
4. Verify content studio has daily loop, KIE image generation, video script generation
5. Verify index.html has products card, platform selector, month selector
6. Verify migration file has products table, column additions, RLS policies
  </action>
  <verify>
    <automated>cd /Users/andrewgershan/Projects/eluxr-v3 && node -e "
const fs = require('fs');
let pass = 0;
let fail = 0;
function check(name, result) { if(result) { pass++; console.log('PASS:', name); } else { fail++; console.log('FAIL:', name); } }

// Check migration exists
check('Migration file exists', fs.existsSync('supabase/migrations/20260303_add_products_and_pipeline_columns.sql'));
const migration = fs.readFileSync('supabase/migrations/20260303_add_products_and_pipeline_columns.sql', 'utf8');
check('Products table in migration', migration.includes('CREATE TABLE public.products'));
check('RLS on products', migration.includes('row level security') || migration.includes('ROW LEVEL SECURITY'));

// Check orchestrator
const orch = JSON.parse(fs.readFileSync('workflows/14-pipeline-orchestrator.json', 'utf8'));
const orchWebhooks = orch.nodes.filter(n => n.type === 'n8n-nodes-base.webhook');
check('Orchestrator has 2+ webhooks', orchWebhooks.length >= 2);
check('Orchestrator has callback webhook', orchWebhooks.some(w => w.parameters.path && w.parameters.path.includes('callback')));

// Check ICP analyzer
const icp = JSON.parse(fs.readFileSync('workflows/01-icp-analyzer.json', 'utf8'));
const icpNodes = icp.nodes.map(n => n.name.toLowerCase());
check('ICP has scraping', icpNodes.some(n => n.includes('jina') || n.includes('scrape')));
check('ICP has callback', icpNodes.some(n => n.includes('callback')));

// Check theme generator
const theme = JSON.parse(fs.readFileSync('workflows/02-theme-generator.json', 'utf8'));
const themeNodes = theme.nodes.map(n => n.name.toLowerCase());
check('Theme has product read', themeNodes.some(n => n.includes('product')));
check('Theme has callback', themeNodes.some(n => n.includes('callback')));

// Check content studio
const studio = JSON.parse(fs.readFileSync('workflows/04-content-studio.json', 'utf8'));
const studioNodes = studio.nodes.map(n => n.name.toLowerCase());
check('Studio has image gen', studioNodes.some(n => n.includes('image') || n.includes('kie')));
check('Studio has callback', studioNodes.some(n => n.includes('callback')));

// Check frontend
const html = fs.readFileSync('index.html', 'utf8');
check('Frontend has products card', html.includes('products-card'));
check('Frontend has platform selector', html.includes('platform-selector') || html.includes('platform-toggle'));
check('Frontend has loadProducts', html.includes('loadProducts'));

console.log('\\nResults:', pass, 'passed,', fail, 'failed');
process.exit(fail > 0 ? 1 : 0);
"</automated>
  </verify>
  <done>Migration applied, all 4 workflows have correct structure, frontend has products card and platform selector, all endpoints return 401 without auth (proving active + protected).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies complete content pipeline end-to-end</name>
  <files></files>
  <action>
User deploys workflow JSONs to n8n Cloud, runs the Supabase migration, and tests the full pipeline by entering a business URL and triggering content generation.
  </action>
  <verify>User confirms pipeline output quality and all 6 steps complete.</verify>
  <done>User approves the pipeline output -- ICP, themes, content items, images, and video scripts all generated correctly.</done>
  <what-built>
Complete content pipeline: Jina AI scraping -> Perplexity research -> Claude ICP synthesis -> Netflix theme generation -> weekly batch content generation with platform-specific posts, hero images, and video scripts. Products card on Setup tab. Platform selector. Callback-pattern orchestrator.
  </what-built>
  <how-to-verify>
1. **Deploy workflows:** Import the 4 updated workflow JSONs (01, 02, 04, 14) into n8n Cloud dashboard. Deactivate old versions, activate new versions.

2. **Run migration:** Execute the SQL in `supabase/migrations/20260303_add_products_and_pipeline_columns.sql` in your Supabase SQL editor.

3. **Test the pipeline:**
   - Open the app (localhost:8080 or deployed URL)
   - Log in
   - On Setup tab, enter your business URL and select industry
   - Select which platforms to generate for
   - Click Generate
   - Watch the 6-step progress:
     Step 1: "Analyzing your business..." (Jina + Perplexity + Claude)
     Step 2: "Creating content themes..." (Netflix model)
     Steps 3-6: "Generating Week 1-4 content..." (posts + images + videos)

4. **Verify results:**
   - Products card appears on Setup tab with scraped products
   - ICP card shows enriched data (content pillars, pain points, etc.)
   - Navigate to Generate tab -- see content items for the month
   - Content items have platform-specific formatting
   - Images appear on content items
   - Some content items have video scripts (3-4 per week)

5. **Check Supabase data:**
   - `products` table has scraped products
   - `icps` table has enriched fields populated
   - `campaigns` table has show_name
   - `themes` table has season_arc and inspirational_theme
   - `content_items` has ~120 items (4 platforms x 30 days), images, video scripts
  </how-to-verify>
  <resume-signal>Type "approved" if the pipeline completes successfully and content looks good, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 6 success criteria from ROADMAP:
1. User enters business URL/industry -> ICP stored in icps table, viewable on Setup tab
2. ICP feeds into themes -> exactly 4 weekly shows per month in themes table
3. Content generation -> 1 post/platform/day with platform-specific formatting
4. Each post has AI-generated image prompt, KIE-generated image URL in content_items
5. Video posts have structured scripts (hook/setup/value/CTA) in content_items
</verification>

<success_criteria>
- Pipeline completes all 6 steps without timeout
- Products visible on Setup tab
- ICP enriched with content pillars, pain points, desires
- 4 weekly themes with show name and progressive arc
- ~120 content items (4 platforms x ~30 days) with correct platform formatting
- ~30 hero images generated and stored
- ~14 video scripts (3-4 per week x 4 weeks) with trending audio
- User confirms pipeline output quality
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-pipeline/06-06-SUMMARY.md`
</output>
