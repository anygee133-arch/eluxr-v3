---
phase: 06-content-pipeline
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - index.html
autonomous: true
requirements: [PIPE-01, PIPE-03, PIPE-06]

must_haves:
  truths:
    - "User can see scraped products on the Setup tab in a dropdown card below the ICP card"
    - "User can edit product names, remove products, and add custom products before generation"
    - "User can select which platforms (LinkedIn, Instagram, X, YouTube) to generate content for"
    - "Pipeline trigger sends platforms and month to the orchestrator along with business_url and industry"
  artifacts:
    - path: "index.html"
      provides: "Products card UI, platform selector, updated pipeline trigger form"
      min_lines: 100
  key_links:
    - from: "index.html"
      to: "Supabase products table"
      via: "supabase.from('products').select()"
      pattern: "products.*select"
    - from: "index.html"
      to: "Orchestrator webhook"
      via: "authenticatedFetch to eluxr-generate-content with platforms + month"
      pattern: "eluxr-generate-content"
---

<objective>
Add frontend UI for product management and platform selection on the Setup tab, and update the pipeline trigger to send new parameters (platforms, month) to the orchestrator.

Purpose: Users need to see and edit their scraped products before content generation. They also need to choose which platforms to generate for. The pipeline trigger needs to forward these new parameters. These frontend changes can be built in parallel with the ICP Analyzer backend (Plan 02) since they both depend only on the schema from Plan 01.

Output: Updated index.html with products card, platform selector, and enriched pipeline trigger
</objective>

<execution_context>
@/Users/andrewgershan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewgershan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-pipeline/06-CONTEXT.md
@.planning/phases/06-content-pipeline/06-RESEARCH.md
@.planning/phases/06-content-pipeline/06-01-SUMMARY.md

@index.html
@config.js

<interfaces>
<!-- Existing patterns from index.html -->
loadICP(): async function fetching from icps table, renders ICP card
renderICPCard(icp): renders structured ICP sections
showICPSkeleton(): skeleton loading animation
escapeHTML(): DOM-based HTML escaping
authenticatedFetch(url, options): JWT-wrapped fetch for n8n webhooks
mapContentItem(): normalizes DB rows to frontend format
window.ELUXR_CONFIG.N8N_WEBHOOK_BASE: base URL for n8n webhooks
window.ELUXR_CONFIG.SUPABASE_URL: Supabase URL
window.supabase: Supabase client instance

<!-- Products table columns (from Plan 01 migration) -->
products: { id, user_id, name, description, price, features, image_url, category, source_url, is_active, created_at, updated_at }

<!-- Orchestrator webhook expects -->
POST /webhook/eluxr-generate-content
Body: { business_url, industry, products, month, platforms }
(Updated from current: { business_url, industry, products })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add products dropdown card and platform selector to Setup tab</name>
  <files>index.html</files>
  <action>
Add three new UI elements to the Setup tab in index.html. Place them after the ICP card section (`icp-card-container`).

**1. Products Dropdown Card (HTML):**
Add a collapsible card with id `products-card-container` AFTER the ICP card container. Style it with the existing glassmorphism pattern (backdrop-filter blur, rgba backgrounds, inset glows). The card should:
- Have a header with "Scraped Products" title, a product count badge, and a collapse/expand chevron
- Be collapsed by default (show header + count, click to expand)
- When expanded, show a list of products with: name (editable inline), description (truncated), price, category badge, active/inactive toggle
- Each product has a "Remove" button (sets is_active=false, visual strikethrough)
- An "Add Custom Product" button at the bottom that adds an empty editable row
- A "Save Changes" button that PATCHes modified products to Supabase

**2. Platform Selector (HTML):**
Add a platform selector section inside the existing form (between the products textarea and the generate button). Use checkbox-style toggle buttons with platform icons/names:
- LinkedIn (checked by default)
- Instagram (checked by default)
- X (checked by default)
- YouTube (checked by default)
Each toggle should be a styled checkbox with the platform name. At least 1 platform must be selected (validate on form submit).

**3. Month Selector (HTML):**
Add a month selector dropdown. Default to the current month. Format: "YYYY-MM" (e.g., "2026-03"). Show display text like "March 2026". Options: current month + next 2 months.

**CSS additions:**
- `.products-card` -- glassmorphism card matching existing ICP card style
- `.products-card-header` -- clickable header with chevron rotation animation
- `.products-list` -- scrollable list, max-height 400px
- `.product-item` -- individual product row with inline editing
- `.product-item.inactive` -- strikethrough + opacity 0.5
- `.platform-selector` -- flex row of toggle buttons
- `.platform-toggle` -- checkbox-style button, checked state uses green accent (#16a34a)
- `.platform-toggle.active` -- green background, white text

**JavaScript additions:**

`loadProducts()` -- async function:
- Fetch products from Supabase: `supabase.from('products').select('*').eq('is_active', true).order('category')`
- Render into the products card
- Called on SIGNED_IN (after loadICP) and when pipeline step 1 completes (ICP analysis includes product scraping)

`renderProductsCard(products)` -- renders the product list HTML:
- Each product: editable name input, description span, price span, category badge, remove button
- Uses escapeHTML() for all text content
- "Add Custom Product" row at bottom
- "Save Changes" button (hidden until edits are made)

`toggleProductsCard()` -- collapse/expand with smooth animation

`saveProductChanges()` -- async function:
- Collect all modified products from the DOM
- PATCH to Supabase products table for updates
- POST for new custom products
- DELETE (or set is_active=false) for removed products
- Show success toast

`getSelectedPlatforms()` -- returns array of selected platform strings (e.g., ["linkedin", "instagram"])

`getSelectedMonth()` -- returns selected month string (e.g., "2026-03")

**Update `generateContent(formData)` function:**
- Add `platforms: getSelectedPlatforms()` to the request body sent to orchestrator
- Add `month: getSelectedMonth()` to the request body
- Validate at least 1 platform selected before submission
- Remove the old `products` textarea value from the body (products now come from Supabase table, not form text)

**Update `handleFormSubmit()` function:**
- Still save business_url and industry to profiles
- Remove products from profile save (they're in their own table now)
- Include platforms and month in the orchestrator call

**Update pipeline progress handler:**
- When step 1 (ICP analysis) completes, call `loadProducts()` to refresh the products card (products were just scraped)
- Already calls `loadICP()` -- keep that

**Expose to window:**
- `window.loadProducts = loadProducts`
- `window.saveProductChanges = saveProductChanges`

Follow existing coding conventions: vanilla JS, no modules, functions in script tags, CSS variables for theming, stagger animations on card reveal.
  </action>
  <verify>
    <automated>cd /Users/andrewgershan/Projects/eluxr-v3 && grep -c "products-card-container\|platform-selector\|loadProducts\|getSelectedPlatforms\|getSelectedMonth" index.html | xargs test 5 -le</automated>
  </verify>
  <done>Setup tab has: products dropdown card (collapsible, editable product list, add/remove/save), platform selector (4 platform toggles, at least 1 required), month selector (current + 2 months). generateContent sends platforms and month to orchestrator. loadProducts called on login and after ICP analysis step completes.</done>
</task>

</tasks>

<verification>
1. Products card renders with glassmorphism styling matching ICP card
2. Platform toggles are functional (check/uncheck, visual state change)
3. Month selector defaults to current month
4. Form validation prevents submission with 0 platforms selected
5. generateContent includes platforms and month in request body
6. loadProducts fetches from Supabase products table
</verification>

<success_criteria>
- Products card visible on Setup tab below ICP card after pipeline runs
- User can edit, remove, and add products inline
- Platform selection defaults to all 4, user can deselect
- Pipeline trigger sends platforms array and month string to orchestrator
- Products card refreshes when ICP analysis step completes
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-pipeline/06-03-SUMMARY.md`
</output>
