---
phase: 04-async-pipeline-real-time-progress-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/14-pipeline-orchestrator.json
autonomous: true

must_haves:
  truths:
    - "POST to /eluxr-generate-content returns HTTP 202 with pipeline_run_id within 2 seconds"
    - "A pipeline_runs row is created in Supabase before the 202 response is sent"
    - "Each of 6 sub-workflow steps updates pipeline_runs.current_step and step_label after completion"
    - "Pipeline completion sets pipeline_runs.status to 'completed'"
    - "Pipeline failure sets pipeline_runs.status to 'failed' with error_message"
    - "Duplicate running pipeline for same user is prevented"
  artifacts:
    - path: "workflows/14-pipeline-orchestrator.json"
      provides: "Pipeline orchestrator n8n workflow"
      contains: "eluxr-generate-content"
  key_links:
    - from: "14-Pipeline-Orchestrator"
      to: "Supabase pipeline_runs table"
      via: "HTTP Request PATCH after each step"
      pattern: "pipeline_runs"
    - from: "14-Pipeline-Orchestrator"
      to: "01-ICP-Analyzer, 02-Theme-Generator, 04-Content-Studio, 11-Image-Generator, 13-Video-Creator, 09-Calendar-Sync"
      via: "Execute Sub-Workflow (Wait=true)"
      pattern: "executeWorkflow"
---

<objective>
Build the n8n Pipeline Orchestrator workflow (14-Pipeline-Orchestrator) that replaces the frontend's sequential 3-call pattern with a single async endpoint. The webhook accepts the generation request, creates a pipeline_runs row in Supabase, returns HTTP 202 immediately with the pipeline_run_id, then continues executing all 6 pipeline steps sequentially -- updating pipeline_runs progress after each step via Supabase REST API.

Purpose: This is the backend half of real-time progress tracking. Without this orchestrator, the frontend has no pipeline_run_id to subscribe to and no real progress events flowing into the database.

Output: A deployed n8n workflow on flowbound.app.n8n.cloud that orchestrates the full content generation pipeline asynchronously with per-step progress persistence.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-async-pipeline-real-time-progress-tracking/04-RESEARCH.md
@.planning/phases/03-workflow-decomposition-backend-bug-fixes/03-05-SUMMARY.md
@.planning/phases/02-authentication/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Pipeline Orchestrator Workflow JSON</name>
  <files>workflows/14-pipeline-orchestrator.json</files>
  <action>
Build the n8n workflow JSON for "ELUXR 14-Pipeline-Orchestrator" with the following node chain:

**Node 1: Webhook** (type: n8n-nodes-base.webhook, typeVersion: 2)
- httpMethod: POST
- path: eluxr-generate-content
- responseMode: responseNode (critical -- allows custom 202 response then continues)
- options.allowedOrigins: "*"

**Node 2: Auth Validator** (type: n8n-nodes-base.executeWorkflow, typeVersion: 1)
- workflowId: S4QtfIKpvhW4mQYG (Auth Validator sub-workflow)
- Pass full input data to sub-workflow

**Node 3: Auth OK? IF** (type: n8n-nodes-base.if, typeVersion: 2)
- Condition: $json.authenticated equals true
- FALSE path -> Node 4 (401 response)
- TRUE path -> Node 5 (dedup check)

**Node 4: 401 Unauthorized** (type: n8n-nodes-base.respondToWebhook, typeVersion: 1.1)
- respondWith: json
- responseBody: { "error": "Unauthorized" }
- options.responseCode: 401

**Node 5: Check Existing Run** (type: n8n-nodes-base.httpRequest, typeVersion: 4.2)
- Check for existing running pipeline for this user
- GET to {{$env.SUPABASE_URL}}/rest/v1/pipeline_runs?user_id=eq.{{$json.user_id}}&status=eq.running&select=id
- Headers: Authorization: Bearer {{$env.SUPABASE_SERVICE_ROLE_KEY}}, apikey: {{$env.SUPABASE_SERVICE_ROLE_KEY}}
- alwaysOutputData: true (so empty results flow through)

**Node 6: Has Running? IF** (type: n8n-nodes-base.if, typeVersion: 2)
- Condition: check if response array has length > 0
- TRUE path -> Node 7 (return existing run_id as 202)
- FALSE path -> Node 8 (create new run)

**Node 7: Return Existing Run** (type: n8n-nodes-base.respondToWebhook, typeVersion: 1.1)
- respondWith: json
- responseBody: { "success": true, "status": "accepted", "pipeline_run_id": existing_id, "resumed": true }
- options.responseCode: 202

**Node 8: Create Pipeline Run** (type: n8n-nodes-base.code, typeVersion: 2)
- Prepare the INSERT body for pipeline_runs:
  ```javascript
  const userId = $input.item.json.user_id;
  const body = $input.item.json.body || {};
  return {
    json: {
      user_id: userId,
      pipeline_type: 'full_pipeline',
      status: 'running',
      current_step: 0,
      total_steps: 6,
      step_label: 'Starting...',
      step_progress: 0,
      started_at: new Date().toISOString(),
      metadata: {
        business_url: body.business_url || '',
        industry: body.industry || '',
        month: body.month || '',
        products: body.products || '',
        brand_voice: body.brand_voice || '',
        platforms: body.platforms || []
      },
      // Pass through for downstream nodes
      body: body,
      email: $input.item.json.email
    }
  };
  ```

**Node 9: INSERT pipeline_runs** (type: n8n-nodes-base.httpRequest, typeVersion: 4.2)
- POST to {{$env.SUPABASE_URL}}/rest/v1/pipeline_runs
- Headers: Authorization: Bearer {{$env.SUPABASE_SERVICE_ROLE_KEY}}, apikey: {{$env.SUPABASE_SERVICE_ROLE_KEY}}, Content-Type: application/json, Prefer: return=representation
- Body: the prepared INSERT data (user_id, pipeline_type, status, current_step, total_steps, step_label, step_progress, started_at, metadata)
- The response returns the created row with its UUID `id`

**Node 10: Extract Run ID** (type: n8n-nodes-base.code, typeVersion: 2)
- Extract pipeline_run_id from INSERT response and preserve body/metadata for sub-workflows:
  ```javascript
  const insertResult = $input.item.json;
  // PostgREST returns array with Prefer: return=representation
  const runId = Array.isArray(insertResult) ? insertResult[0].id : insertResult.id;
  const metadata = Array.isArray(insertResult) ? insertResult[0].metadata : insertResult.metadata;
  return {
    json: {
      pipeline_run_id: runId,
      user_id: Array.isArray(insertResult) ? insertResult[0].user_id : insertResult.user_id,
      body: metadata,
      email: $input.item.json.email || metadata.email
    }
  };
  ```

**Node 11: Respond 202** (type: n8n-nodes-base.respondToWebhook, typeVersion: 1.1)
- respondWith: json
- responseBody: `={{ JSON.stringify({ success: true, status: "accepted", pipeline_run_id: $json.pipeline_run_id }) }}`
- options.responseCode: 202
- This node has an OUTPUT that continues to the pipeline steps

**After the 202 response, the workflow continues executing:**

For each of the 6 steps, create a pair: [Update Step N] -> [Execute Sub-Workflow N]

Step labels and sub-workflow mapping:
1. "Analyzing your business..." -> 01-ICP-Analyzer
2. "Creating content themes..." -> 02-Theme-Generator
3. "Writing posts..." -> 04-Content-Studio
4. "Generating images..." -> 11-Image-Generator
5. "Creating videos..." -> 13-Video-Creator
6. "Syncing calendar..." -> 09-Calendar-Sync

**For each "Update Step N" node** (type: n8n-nodes-base.httpRequest, typeVersion: 4.2):
- PATCH to {{$env.SUPABASE_URL}}/rest/v1/pipeline_runs?id=eq.{{$json.pipeline_run_id}}
- Headers: Authorization: Bearer {{$env.SUPABASE_SERVICE_ROLE_KEY}}, apikey: {{$env.SUPABASE_SERVICE_ROLE_KEY}}, Content-Type: application/json, Prefer: return=representation
- Body: { "current_step": N, "step_label": "<label>", "step_progress": 0, "status": "running", "updated_at": "<ISO timestamp>" }

**For each "Execute Sub-Workflow" node** (type: n8n-nodes-base.executeWorkflow, typeVersion: 1):
- workflowId: look up from the workflow files (use search_nodes or n8n MCP to find IDs)
- Wait=true (default, DO NOT use Wait=false -- known bug)
- Pass through user_id, body (business_url, industry, month, etc.), and pipeline_run_id
- Note: Sub-workflows expect specific input shapes. ICP Analyzer expects the form fields directly. Theme Generator expects session_id/icp_summary. Content Studio expects session_id/date. Image/Video/Calendar may need different data. Check the webhook body expectations of each sub-workflow and map accordingly.

**After all 6 steps complete:**

**Node "Mark Complete"** (type: n8n-nodes-base.httpRequest, typeVersion: 4.2):
- PATCH to {{$env.SUPABASE_URL}}/rest/v1/pipeline_runs?id=eq.{{$json.pipeline_run_id}}
- Body: { "status": "completed", "current_step": 6, "step_label": "Complete!", "step_progress": 100, "completed_at": "<ISO timestamp>", "updated_at": "<ISO timestamp>" }

**Error handling:** Add an error workflow or use n8n's "On Error" setting on the workflow to catch failures. At minimum, use the workflow's onError setting to execute a cleanup that PATCHes pipeline_runs with status: 'failed'. If n8n workflow-level error handler is not straightforward, add a note in the summary that per-step error handling should be added in a gap-closure plan if needed.

**Critical things to get right:**
- responseMode on Webhook MUST be "responseNode" (not "lastNode" or "onReceived")
- INSERT pipeline_runs MUST complete BEFORE Respond 202 in the node graph
- Respond 202 MUST have an output connection to the first Update Step node
- All HTTP Request nodes to Supabase use service_role key via $env.SUPABASE_SERVICE_ROLE_KEY
- All Execute Sub-Workflow nodes use Wait=true (default)
- Node connections form a single linear chain after the Respond 202 node

**Deploy using n8n MCP:** Use n8n_create_workflow to push this workflow to flowbound.app.n8n.cloud. The workflow should be created as INACTIVE initially (user will activate manually after review, consistent with Phase 3 cutover pattern).

Save the workflow JSON to workflows/14-pipeline-orchestrator.json for reference.
  </action>
  <verify>
1. The workflow JSON is valid and saved to workflows/14-pipeline-orchestrator.json
2. The workflow is deployed to n8n Cloud (verify via MCP or provide workflow URL)
3. Confirm the node chain: Webhook -> Auth Validator -> IF -> Create Run -> INSERT -> Respond 202 -> [6x Update+Execute pairs] -> Mark Complete
4. Verify responseMode is "responseNode" on the Webhook node
5. Verify all Supabase HTTP Request nodes use $env.SUPABASE_SERVICE_ROLE_KEY
  </verify>
  <done>
A "14-Pipeline-Orchestrator" workflow exists on n8n Cloud with webhook path /eluxr-generate-content. It follows the pattern: authenticate -> check dedup -> create pipeline_runs row -> respond 202 with pipeline_run_id -> execute 6 sub-workflows sequentially with progress updates -> mark complete. The workflow is deployed (inactive, ready for user activation).
  </done>
</task>

<task type="auto">
  <name>Task 2: Test Orchestrator Core Mechanics</name>
  <files>workflows/14-pipeline-orchestrator.json</files>
  <action>
After the workflow is deployed and activated (or ask user to activate), verify the core async mechanics work:

1. **Test the respond-then-continue pattern:**
   - Make a test webhook call (unauthenticated first, expect 401)
   - This confirms the webhook is active and auth is working

2. **Verify the workflow structure via n8n MCP:**
   - Use the MCP tools to read the deployed workflow and confirm:
     - Webhook node has responseMode: responseNode
     - Respond 202 node has output connection to the first Update Step
     - All 6 Execute Sub-Workflow nodes reference valid workflow IDs
     - All Supabase HTTP Request nodes have correct URL patterns

3. **Document the deployed workflow ID** for the frontend plan to reference.

4. **Create a test script** (scripts/test-orchestrator.sh) that:
   - Sends an unauthenticated POST to /eluxr-generate-content, expects 401
   - Documents the expected authenticated flow (cannot test without valid JWT in script)

If the workflow needs adjustments (wrong sub-workflow IDs, incorrect node wiring), fix them via MCP and update the local JSON.

**Important:** The sub-workflows expect specific data shapes from their webhook triggers. The orchestrator's Execute Sub-Workflow nodes pass data differently than direct webhook calls. The sub-workflows may need their trigger nodes adjusted to accept data from Execute Sub-Workflow (which uses the Workflow Trigger node input, not Webhook body). If sub-workflow compatibility is an issue, document it clearly -- the E2E verification plan (04-03) will handle full integration testing.
  </action>
  <verify>
1. `curl -X POST https://flowbound.app.n8n.cloud/webhook/eluxr-generate-content -H "Content-Type: application/json" -d '{}'` returns 401
2. The workflow is visible on n8n Cloud with all nodes connected
3. scripts/test-orchestrator.sh exists and is executable
4. Deployed workflow ID is documented
  </verify>
  <done>
The Pipeline Orchestrator is deployed on n8n Cloud, returns 401 for unauthenticated requests (proving webhook active + auth working), and the node chain is verified correct. Sub-workflow IDs are confirmed. Test script exists for verification reference.
  </done>
</task>

</tasks>

<verification>
- The /eluxr-generate-content webhook endpoint is active on n8n Cloud
- Unauthenticated requests return 401 (Auth Validator integrated)
- Workflow node chain follows: Webhook -> Auth -> IF -> Dedup Check -> Create Run -> INSERT -> Respond 202 -> [6x Update+Execute] -> Mark Complete
- Webhook responseMode is "responseNode"
- All Supabase operations use service_role key via environment variable
- Execute Sub-Workflow nodes reference correct workflow IDs for all 6 steps
</verification>

<success_criteria>
The Pipeline Orchestrator workflow is deployed to n8n Cloud with the async respond-then-continue pattern. It creates a pipeline_runs row, returns 202 with the run ID immediately, then executes all 6 pipeline steps with per-step progress updates to Supabase. The workflow handles auth, deduplication, and has the structure for error handling.
</success_criteria>

<output>
After completion, create `.planning/phases/04-async-pipeline-real-time-progress-tracking/04-01-SUMMARY.md`
</output>
