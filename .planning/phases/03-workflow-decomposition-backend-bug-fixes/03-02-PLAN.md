---
phase: 03-workflow-decomposition-backend-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - n8n-workflow-remote
autonomous: true

must_haves:
  truths:
    - "Sub-workflow 01-ICP-Analyzer exists in n8n Cloud with its own webhook, Auth Validator integration, and Supabase UPSERT for ICP data"
    - "Sub-workflow 02-Theme-Generator exists with two-step campaign UPSERT + themes batch INSERT to Supabase"
    - "Sub-workflow 03-Themes-List exists with Supabase SELECT (joined themes + campaigns) replacing Google Sheets read"
    - "Sub-workflow 04-Content-Studio exists with FIXED Switch node (mutually exclusive routing, first-match mode, normalized content_type)"
    - "Sub-workflow 05-Content-Submit exists with Supabase INSERT for content_items replacing Google Sheets append"
    - "No Google Sheets nodes exist in any of the 5 sub-workflows"
    - "All 5 sub-workflows follow the standard pattern: Webhook -> Auth Validator -> IF -> Business Logic -> Respond"
    - "The Switch node in 04-Content-Studio routes to exactly one branch per content item (PIPE-07 fix)"
  artifacts:
    - path: "workflows/01-icp-analyzer.json"
      provides: "Standalone ICP analysis sub-workflow with Supabase UPSERT"
    - path: "workflows/02-theme-generator.json"
      provides: "Theme generation sub-workflow with two-step Supabase insert"
    - path: "workflows/03-themes-list.json"
      provides: "Themes read endpoint with Supabase SELECT"
    - path: "workflows/04-content-studio.json"
      provides: "Content generation pipeline with fixed Switch routing (PIPE-07)"
    - path: "workflows/05-content-submit.json"
      provides: "Content submission endpoint with Supabase INSERT"
  key_links:
    - from: "Auth Validator sub-workflow"
      to: "Each sub-workflow"
      via: "Execute Sub-Workflow (S4QtfIKpvhW4mQYG)"
      pattern: "user_id in data pipeline"
    - from: "Each sub-workflow"
      to: "Supabase REST API"
      via: "HTTP Request with dual-header auth"
      pattern: "SUPABASE_CREDENTIAL_PATTERN from 03-01"
    - from: "04-Content-Studio Switch"
      to: "Text/Image/Video branches"
      via: "First-match mode with exact content_type equality"
      pattern: "CONTENT_TYPE_VALUES from 03-01"
---

<objective>
Build and deploy the first 5 domain sub-workflows (01-ICP-Analyzer through 05-Content-Submit), replacing all their Google Sheets nodes with Supabase queries and fixing the Switch node routing bug (PIPE-07) in 04-Content-Studio.

Purpose: These 5 workflows cover the entire content generation pipeline -- from ICP analysis through theme generation, content creation, and submission. Building them first validates the sub-workflow pattern at scale and fixes the most impactful bug (Switch routing). The Content Studio workflow is the most complex sub-workflow and contains the PIPE-07 bug, so extracting and fixing it here validates the approach before simpler workflows.

Output: 5 independently deployable n8n sub-workflows, each with its own webhook, Auth Validator integration, and Supabase queries instead of Google Sheets. The Switch node routes mutually exclusively. All workflows saved as JSON artifacts and deployed to n8n Cloud.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow-decomposition-backend-bug-fixes/03-RESEARCH.md
@.planning/phases/03-workflow-decomposition-backend-bug-fixes/03-01-SUMMARY.md
@.planning/phases/02-authentication/02-02-SUMMARY.md
@workflows/eluxr-social-media-action-v2-authenticated.json
@workflows/eluxr-auth-validator.json
@supabase/migrations/20260228044505_create_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 01-ICP-Analyzer sub-workflow</name>
  <files>n8n-workflow-remote</files>
  <action>
Extract the ICP analysis pipeline from the monolith into a standalone sub-workflow. Read 03-01-SUMMARY.md for SUPABASE_CREDENTIAL_PATTERN.

**Nodes to extract from monolith:**
- Phase 1 -- Webhook Trigger1 (webhook: /eluxr-phase1-analyzer)
- Auth Validate (eluxr-phase1-analyzer) + Auth OK? + 401 Unauthorized
- Prepare ICP Prompt (Code node)
- Perplexity -- Market Research1 (HTTP Request)
- Claude -- ICP Generator1 (HTTP Request)
- Parse ICP Response1 (Code node)
- Save ICP to Google Sheets1 -> **REPLACE with Supabase UPSERT**
- Phase 1 -- Response1 (Respond to Webhook)

**Google Sheets replacement:**
Replace "Save ICP to Google Sheets1" (appendOrUpdate) with an HTTP Request node:
- Method: POST
- URL: `https://llpnwaoxisfwptxvdfed.supabase.co/rest/v1/icps`
- Headers: Authorization + apikey (service_role), Prefer: resolution=merge-duplicates,return=representation, Content-Type: application/json
- Body: Map the parsed ICP fields to the icps table schema (user_id from Auth Validator, business_url, industry, icp_summary, demographics, psychographics, content_preferences, competitors, content_opportunities, recommended_hashtags)
- The UNIQUE(user_id) constraint handles the upsert logic

**Build the workflow JSON** using the standard pattern:
1. Webhook (POST /eluxr-phase1-analyzer)
2. Execute Sub-Workflow (Auth Validator, workflowId: S4QtfIKpvhW4mQYG)
3. IF (authenticated = true)
4. TRUE path: Prepare ICP Prompt -> Perplexity -> Claude -> Parse ICP -> Supabase UPSERT -> Respond (200)
5. FALSE path: Respond (401)

**Use `search_nodes`** to confirm exact node type names and versions before building JSON.

**Deploy** via `n8n_create_workflow` with name "01-ICP-Analyzer".

Save the workflow JSON to `workflows/01-icp-analyzer.json`.
  </action>
  <verify>
1. Workflow "01-ICP-Analyzer" exists on n8n Cloud
2. Webhook path is /eluxr-phase1-analyzer
3. Auth Validator is called as first step
4. Supabase UPSERT replaces Google Sheets appendOrUpdate
5. No Google Sheets nodes in the workflow
6. The user_id from Auth Validator is used in the Supabase upsert body
7. Workflow JSON saved locally
  </verify>
  <done>
01-ICP-Analyzer sub-workflow is built, deployed, and saved with Supabase UPSERT replacing Google Sheets. Auth Validator integrated. No Sheets nodes remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build 02-Theme-Generator sub-workflow</name>
  <files>n8n-workflow-remote</files>
  <action>
Extract the theme generation pipeline from the monolith into a standalone sub-workflow.

**Nodes to extract:**
- Phase 2 -- Webhook Trigger (webhook: /eluxr-phase2-themes)
- Auth Validate (eluxr-phase2-themes) + Auth OK? + 401 Unauthorized
- Claude -- Netflix Theme Generator (HTTP Request)
- Parse & Split Theme Days (Code node)
- Save Themes to Google Sheets -> **REPLACE with two-step Supabase insert**
- Phase 2 -- Response (Respond to Webhook)

**Google Sheets replacement (two-step, from 03-01-SUMMARY THEME_INSERT_PATTERN):**

Replace "Save Themes to Google Sheets" with a multi-node flow:

Node A: "Prepare Campaign Data" (Code node)
- Extract month and user_id from the parsed theme data
- Format for campaign UPSERT

Node B: "Supabase -- UPSERT Campaign" (HTTP Request)
- Method: POST
- URL: `https://llpnwaoxisfwptxvdfed.supabase.co/rest/v1/campaigns`
- Headers: Prefer: resolution=merge-duplicates,return=representation
- Body: { user_id, month, status: "active" }
- Returns the campaign row with its id

Node C: "Prepare Themes Batch" (Code node)
- Take the campaign_id from Node B
- Map each theme day to a themes table row with the campaign_id FK
- Include: user_id, campaign_id, week_number, theme_name, theme_description, show_concept, hook, content_types

Node D: "Supabase -- INSERT Themes" (HTTP Request)
- Method: POST
- URL: `https://llpnwaoxisfwptxvdfed.supabase.co/rest/v1/themes`
- Headers: Prefer: return=representation
- Body: Array of theme objects

**Build the full workflow** following the standard webhook + auth pattern.

**Deploy** via `n8n_create_workflow` with name "02-Theme-Generator".

Save the workflow JSON to `workflows/02-theme-generator.json`.
  </action>
  <verify>
1. Workflow "02-Theme-Generator" exists on n8n Cloud
2. Campaign UPSERT happens before themes INSERT (correct FK ordering)
3. Campaign_id from UPSERT is used as FK in themes INSERT
4. No Google Sheets nodes
5. user_id injected from Auth Validator into both campaign and themes
6. Workflow JSON saved locally
  </verify>
  <done>
02-Theme-Generator sub-workflow is built with two-step Supabase insert (campaign UPSERT -> themes batch INSERT). No Sheets nodes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build 03-Themes-List sub-workflow</name>
  <files>n8n-workflow-remote</files>
  <action>
Extract the themes list read endpoint from the monolith.

**Nodes to extract:**
- Themes List -- Webhook (webhook: /eluxr-themes-list)
- Auth Validate (eluxr-themes-list) + Auth OK? + 401 Unauthorized
- Read Themes Sheet -> **REPLACE with Supabase SELECT**
- Format Themes Response (Code node)
- Themes List -- Response (Respond to Webhook)

**Google Sheets replacement:**
Replace "Read Themes Sheet" with an HTTP Request node:
- Method: GET
- URL: `https://llpnwaoxisfwptxvdfed.supabase.co/rest/v1/themes?user_id=eq.{{ $json.user_id }}&select=*,campaigns(month,status)&order=created_at.desc`
- This uses PostgREST's embedded resource syntax to join themes with campaigns

**Adapt the "Format Themes Response" Code node** to handle the Supabase response format instead of Google Sheets format. The Supabase response will be an array of theme objects with nested campaign data, while Google Sheets returned flat rows.

**Deploy** via `n8n_create_workflow` with name "03-Themes-List".

Save to `workflows/03-themes-list.json`.
  </action>
  <verify>
1. Workflow "03-Themes-List" exists on n8n Cloud
2. Supabase SELECT with joined campaigns replaces Google Sheets read
3. Format Themes Response handles Supabase response format
4. No Google Sheets nodes
5. user_id filter ensures tenant isolation
6. Workflow JSON saved locally
  </verify>
  <done>
03-Themes-List sub-workflow reads from Supabase with campaign join. No Sheets nodes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Build 04-Content-Studio sub-workflow with PIPE-07 Switch fix</name>
  <files>n8n-workflow-remote</files>
  <action>
This is the most complex sub-workflow and includes the PIPE-07 bug fix. Extract the content generation pipeline from the monolith.

**Nodes to extract:**
- Phase 4 -- Webhook Trigger (webhook: /eluxr-phase4-studio)
- Daily Content Trigger (6 AM) -- scheduleTrigger (keep for automated daily runs)
- Auth Validate (eluxr-phase4-studio) + Auth OK? + 401 Unauthorized
- Merge Trigger Sources (Code node)
- Read Today's Theme -> **REPLACE with Supabase SELECT**
- Filter Today's Content (Code node)
- Loop Over Days (splitInBatches)
- Route: Text / Image / Video -> **FIX PIPE-07 BUG**
- Claude -- Write Post Content (HTTP Request, text branch)
- KIE -- Generate Content Image (HTTP Request, image branch)
- Claude -- Video Script Generator (HTTP Request, video branch)
- Merge All Content (Code node)
- Save to Approval Queue Sheet -> **REPLACE with Supabase INSERT**
- Phase 3 -- Response (Respond to Webhook)

**PIPE-07 Bug Fix -- Switch Node:**

Read 03-01-SUMMARY.md for CONTENT_TYPE_VALUES. Reconfigure the Switch node:

1. Set `options.allMatchingOutputs` to `false` (first-match mode only)
2. Rewrite rules with exact equality checks:
   - Rule 0 (Text): content_type equals the text value (e.g., "text" or "Text Post")
   - Rule 1 (Image): content_type equals the image value (e.g., "image" or "Image Post")
   - Rule 2 (Video): content_type contains "video" (case-insensitive, since video types may vary)
3. Add a fallback output (`fallbackOutput: "extra"`) for unmatched items
4. Add a Code node before the Switch to normalize content_type to lowercase:

```javascript
const item = $input.item.json;
item.content_type = (item.content_type || '').toLowerCase().trim();
return { json: item };
```

**Google Sheets replacements:**

1. "Read Today's Theme" -> Supabase SELECT:
   - GET `https://llpnwaoxisfwptxvdfed.supabase.co/rest/v1/themes?user_id=eq.{{ user_id }}&select=*,campaigns(month)`
   - Filter by scheduled_date if applicable, or by current week_number

2. "Save to Approval Queue Sheet" -> Supabase INSERT into content_items:
   - POST `https://llpnwaoxisfwptxvdfed.supabase.co/rest/v1/content_items`
   - Body: { user_id, campaign_id, theme_id, title, content, content_type, platform, scheduled_date, status: "pending_review", image_url, video_url, image_prompt }

**Note on Daily Content Trigger:** This scheduleTrigger node fires daily at 6 AM. In the sub-workflow, it needs to be included alongside the webhook trigger. The "Merge Trigger Sources" Code node handles both trigger paths.

**Deploy** via `n8n_create_workflow` with name "04-Content-Studio".

Save to `workflows/04-content-studio.json`.
  </action>
  <verify>
1. Workflow "04-Content-Studio" exists on n8n Cloud
2. Switch node has allMatchingOutputs=false (first-match mode)
3. Switch rules use exact equality for text and image, contains for video
4. content_type is normalized to lowercase before Switch
5. Fallback output exists for unmatched items
6. Read Today's Theme replaced with Supabase SELECT
7. Save to Approval Queue Sheet replaced with Supabase INSERT
8. No Google Sheets nodes
9. Both webhook and schedule triggers are included
10. Test: send a text content_type item -- verify only text branch fires
11. Test: send an image content_type item -- verify only image branch fires
12. Test: send a video content_type item -- verify only video branch fires
  </verify>
  <done>
04-Content-Studio is built with PIPE-07 Switch fix (first-match mode, exact equality rules, lowercase normalization, fallback output). Both Sheets nodes replaced with Supabase. Routing is mutually exclusive.
  </done>
</task>

<task type="auto">
  <name>Task 5: Build 05-Content-Submit sub-workflow</name>
  <files>n8n-workflow-remote</files>
  <action>
Extract the content submission endpoint from the monolith.

**Nodes to extract:**
- Submit Content -- Webhook (webhook: /eluxr-phase5-submit)
- Auth Validate (eluxr-phase5-submit) + Auth OK? + 401 Unauthorized
- Format Queue Item (Code node)
- Save to Queue Sheet -> **REPLACE with Supabase INSERT**
- Submit -- Response (Respond to Webhook)

**Google Sheets replacement:**
Replace "Save to Queue Sheet" with Supabase INSERT:
- Method: POST
- URL: `https://llpnwaoxisfwptxvdfed.supabase.co/rest/v1/content_items`
- Headers: Prefer: return=representation
- Body: { user_id (from Auth Validator), title, content, content_type, platform, scheduled_date, status: "pending_review", image_url, video_url, image_prompt }

**Adapt "Format Queue Item" Code node** to output the Supabase-compatible object format instead of the Google Sheets row format.

**Deploy** via `n8n_create_workflow` with name "05-Content-Submit".

Save to `workflows/05-content-submit.json`.
  </action>
  <verify>
1. Workflow "05-Content-Submit" exists on n8n Cloud
2. Supabase INSERT replaces Google Sheets append
3. user_id from Auth Validator included in insert body
4. No Google Sheets nodes
5. Responds with the created content item
6. Workflow JSON saved locally
  </verify>
  <done>
05-Content-Submit sub-workflow submits content to Supabase content_items table. No Sheets nodes.
  </done>
</task>

</tasks>

<verification>
1. All 5 sub-workflows (01-05) exist on n8n Cloud with correct webhook paths
2. Each follows the standard pattern: Webhook -> Auth Validator -> IF -> Business Logic -> Respond
3. Zero Google Sheets nodes across all 5 workflows (5 Sheets nodes replaced with Supabase)
4. 04-Content-Studio Switch node routes mutually exclusively (PIPE-07 fixed)
5. 02-Theme-Generator uses two-step campaign/themes insert with FK
6. user_id from Auth Validator flows through all data operations
7. All workflow JSONs saved locally in workflows/ directory
</verification>

<success_criteria>
- 5 sub-workflows deployed and saved: 01-ICP-Analyzer, 02-Theme-Generator, 03-Themes-List, 04-Content-Studio, 05-Content-Submit
- Switch node in 04-Content-Studio uses first-match mode with exact equality checks (PIPE-07 fix verified)
- No Google Sheets nodes in any of the 5 workflows
- All Supabase operations use validated credential pattern from 03-01
- Two-step campaign/themes insert works with correct FK ordering
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-decomposition-backend-bug-fixes/03-02-SUMMARY.md`

CRITICAL: The SUMMARY must include:
1. **DEPLOYED_WORKFLOWS**: List of 5 workflow names and their n8n Cloud IDs
2. **WEBHOOK_PATHS**: Mapping of webhook paths to workflow IDs (needed for frontend URL update in Plan 05)
3. **PIPE_07_STATUS**: Confirmation that Switch routing is fixed with test results
4. **SHEETS_NODES_REPLACED**: Count of Google Sheets nodes replaced with Supabase in this plan
5. **SWITCH_CONFIG**: The exact Switch node configuration for reference
</output>
